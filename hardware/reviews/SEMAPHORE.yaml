# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: reviewer_turn
current_cp: cp3-placement
current_branch: hw/cp3-placement
active_packet: hardware/reviews/cp3_placement_review.md
iteration: 13

max_iterations_per_cp: 1000

# Reviewer Patch Authority (hardware/reviews/REVIEWER_PATCH_POLICY.md):
# git author names that count as the reviewer. A reviewer commit that
# changes code must carry a Reviewer-Patch trailer and satisfy the
# zero-delta invariant; enforced by tools/reviewer_patch_check.py.
reviewer_git_authors: [voxelisKW]
# Enforcement epoch: RPA binds commits from the policy's own commit
# forward. An immutable sha, never a mutable ref — a range like
# origin/main..HEAD makes the verdict depend on which clone runs it
# (CP3 F11). Pre-policy reviewer patches are legitimized retroactively
# by an RPA-ACCEPTED line, not by moving this.
rpa_policy_base: 3e4c097

last_updated_at: 2026-08-05T20:24:37Z
last_updated_by: claude-designer

note: >
  DESIGNER iteration 12: F13 fixed (handoff passes NO range; checker is
  sole owner of epoch selection; integration-poisoned with origin/main
  moved 6 commits away — standalone and handoff verdicts identical).
  F14 fixed as a CLASS: every gate input the guarded party can write is
  attack surface. Swept all four; two you did not name — emptying
  reviewer_git_authors skipped every check, and RPA-ACCEPTED lines in
  the reviewer-written packet let a patch ACCEPT ITSELF. Now
  POLICY_BASE + REVIEWER_AUTHORS are pinned in enforcement code (in
  SELF -> SCRUTINY on reviewer edits), semaphore copies are documentary
  + equality-checked, and sign-off is verified by git AUTHORSHIP via
  blame. NOTE: semaphore commits are deliberately NOT flagged as code —
  that fires on every legitimate turn flip; the semaphore is powerless
  instead (control in the drill proves an iteration bump still passes).
  active_packet is documented accepted residual. Drill:
  iter12/rpa_trust_model_drill.txt. Board bytes unchanged 448d59a276df.
  Skill: pcb-design-review v1.6.0.


user_pause_reasons_seen_this_run: []
