# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: claude_turn
current_cp: cp4-display-placement
current_branch: hw/cp4-display-placement
active_packet: hardware/reviews/cp4_display_placement_review.md
iteration: 2

max_iterations_per_cp: 1000

# Reviewer Patch Authority (hardware/reviews/REVIEWER_PATCH_POLICY.md):
# git author names that count as the reviewer. A reviewer commit that
# changes code must carry a Reviewer-Patch trailer and satisfy the
# zero-delta invariant; enforced by tools/reviewer_patch_check.py.
reviewer_git_authors: [voxelisKW]
# Enforcement epoch: RPA binds commits from the policy's own commit
# forward. An immutable sha, never a mutable ref — a range like
# origin/main..HEAD makes the verdict depend on which clone runs it
# (CP3 F11). Pre-policy reviewer patches are legitimized retroactively
# by an RPA-ACCEPTED line, not by moving this.
rpa_policy_base: 3e4c097

last_updated_at: 2026-08-06T05:20:46Z
last_updated_by: agent-reviewer

note: >
  REVIEWER iteration 1: NEEDS CHANGES (1 blocker, 3 important). Mandatory
  display rebuild and full handoff gate fail on J1 orientation. Root cause:
  the back-side center helper still negates local X while the writer/gates
  correctly negate local Y, so J1/U1 are not at their requested centers;
  J1 at rot=0 is north/south, not west-facing. Corrected-center/rotation
  probing exposes J1 conflicts with BTN1, R5, and U1, requiring re-placement.
  Live SKU resolution also found J2 DK 455-1710-ND orders rejected top-entry
  B8B (use 455-1725-ND) and J-USB Mouser 640-USB4085-GF-A orders the retired
  edge part (use 640-USB4115-03-C). J1/J-USB bodies are absent from both
  committed and reviewer 3D renders, buttons render H13 rather than selected
  15 mm, and the TS02 footprint's 1.1 mm drills are not the claimed exact
  match to the 1.0 mm recommendation. See packet 8.1 and reviewer REPORT.md.


user_pause_reasons_seen_this_run: []
