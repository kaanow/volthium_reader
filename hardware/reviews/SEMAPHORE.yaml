# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: reviewer_turn
current_cp: cp3-placement
current_branch: hw/cp3-placement
active_packet: hardware/reviews/cp3_placement_review.md
iteration: 11

max_iterations_per_cp: 1000

# Reviewer Patch Authority (hardware/reviews/REVIEWER_PATCH_POLICY.md):
# git author names that count as the reviewer. A reviewer commit that
# changes code must carry a Reviewer-Patch trailer and satisfy the
# zero-delta invariant; enforced by tools/reviewer_patch_check.py.
reviewer_git_authors: [voxelisKW]
# Enforcement epoch: RPA binds commits from the policy's own commit
# forward. An immutable sha, never a mutable ref — a range like
# origin/main..HEAD makes the verdict depend on which clone runs it
# (CP3 F11). Pre-policy reviewer patches are legitimized retroactively
# by an RPA-ACCEPTED line, not by moving this.
rpa_policy_base: 3e4c097

last_updated_at: 2026-07-31T14:03:13Z
last_updated_by: claude-designer

note: >
  DESIGNER iteration 10: F11/F12 fixed at root. F11's real cause was
  deeper than reported — the default range origin/main..HEAD is a
  MUTABLE ref, so the gate's verdict depended on which clone ran it;
  now an immutable epoch (rpa_policy_base: 3e4c097). F12: suffix list
  replaced by whole-build-directory denial, held equal to
  handoff_check.BUILD_DIRS by a drift self-test. Added beyond the
  findings: host-evidence requirement enforced, SCRUTINY flag when a
  patch touches the gate/policy/handoff_check, merges skipped, and
  finding-ids now match both 'F11' and 'Finding 11' spellings (the
  drill caught that one). Drill: iter10/rpa_drill_v2.txt. Handoff gate
  CLEAN incl. RPA (exit 0, default range) on macOS — please confirm on
  Windows. Board bytes unchanged 448d59a276df. Thanks for the two-run
  F10 acceptance; declining to patch F11/F12 as designer-reproducible
  was exactly right. Skill: pcb-design-review v1.5.1.


user_pause_reasons_seen_this_run: []
