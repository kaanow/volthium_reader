# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: claude_turn

current_cp: cp1-architecture
current_branch: hw/cp1-architecture
active_packet: hardware/reviews/cp1_design_baseline.md
iteration: 0

max_iterations_per_cp: 1000

last_updated_at: 2026-06-17T00:00:00Z
last_updated_by: claude

note: >
  CP1 RE-OPENED (decisions.md D18). The project nominally reached CP6
  fab-ready, but DR-1/DR-2 proved the defects were CP1/CP2 architecture
  decisions that no gate caught (D17). We revert the mindset to CP1 and
  re-run the full gate sequence with the D17 engineering-correctness gate
  now in place, ignoring sunk cost.

  Superseded (D18): all board artifacts — placement (CP3/CP4), routing +
  pours (CP5), fab outputs (CP6). They were built against pre-DR
  schematics and will be regenerated after the schematics are
  re-validated. Carried forward: the framework (D11/D13/D16/D17 +
  ENGINEERING_REVIEW.md + DESIGN_REVIEW_ITEMS.md), the audit tooling, the
  DR-1/DR-2 schematic fixes, and the readability results.

  Claude's CP1 task: run the clean-sheet architecture engineering review
  per hardware/reviews/ENGINEERING_REVIEW.md across all domains (power
  input, regulation, comms, MCU, sensing, connectors). Re-derive each
  block from first principles, measure the existing design against it as
  a CANDIDATE (not a baseline), fix clear errors, log judgment calls in
  DESIGN_REVIEW_ITEMS.md, and produce the CP1 packet
  (cp1_design_baseline.md). Reach genuine excellence before any codex
  handoff — codex is a second set of eyes for what was missed, not a
  crutch.
