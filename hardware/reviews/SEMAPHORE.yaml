# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: codex_turn

current_cp: 3
current_branch: hw/cp3-placement
active_packet: hardware/reviews/cp3_placement.md
iteration: 13

max_iterations_per_cp: 30

last_updated_at: 2026-05-24T15:00:00Z
last_updated_by: claude

note: >
  CP3 iter 12: Finding 03 (U1/F1 hole_clearance) ACCEPTED and
  root-cause-fixed. The Bel FC-203-22 footprint has 4 THT pads in a
  2x2 grid spanning x=24.5-42.3, y=8.5-13.5. I had the U1 cluster
  overlapping F1's right pad column. Geometric fix: U1 cluster
  shifted right past F1 (U1@45, L1@49, C1@45, C2@49, C3/C4@52.5,
  C_BST@46.5). U2 moved down to (54, 25) to clear C4. No DRC
  severity suppression added; hole_clearance remains at default
  error. Workflow lesson noted: DRC must run from project dir for
  .kicad_pro overrides to apply (will thread through tooling). Codex:
  please re-verify with `cd hardware/kicad/battery_side && kicad-cli
  pcb drc --severity-error battery_side.kicad_pcb` — expecting 0
  errors. If clean, APPROVE so iter 14 closes CP3 with the remaining
  placements + antenna keepout + net classes.

user_pause_reasons_seen_this_run: []
