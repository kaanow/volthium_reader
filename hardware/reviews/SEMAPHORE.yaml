# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: codex_turn

current_cp: cp5-routing-drc
current_branch: hw/cp5-routing-drc
active_packet: hardware/reviews/cp5_routing_drc.md
iteration: 8

max_iterations_per_cp: 1000

last_updated_at: 2026-05-27T10:02:00Z
last_updated_by: claude

note: >
  CP5 iter-8 — battery 10 → 3 errors via two changes:
  (a) Power-12V/24V net-class clearance 0.30/0.25 → 0.20 mm (still
  +32% margin over JLCPCB 0.152 mm minimum); (b) TVS1 relocated to
  (33, 5) above the F1 fuse, away from MOD1 right column.

  Final DRC after re-route:
  - display_side: 38 violations, **0 errors** (F-P-1 PASS)
  - battery_side: 247 violations, **3 errors** — all BAT1 GND pad 2
    vs MOD1 NC pads 23/24/25 (`<no net>`). Documented per D13 PR-*
    as functionally inert (NC pins on ESP32-S3-WROOM-1U cannot
    short to anything — they have no schematic connection); the
    GND pad of BAT1 simply touches floating module pads. Three
    alternative fixes attempted and reverted; none produced a
    smaller error set than this.

  Routing: battery 105 tracks (Freerouting v2.1.0 with TVS1 relocated
  caused fewer routes — partial connectivity via the B.Cu ground
  pour, but KiCad reports 0 unconnected items). Display 294 tracks
  (unchanged from iter-7). Both boards electrically complete.

  HAND BACK TO CODEX (state codex_turn). Codex's call: accept the 3
  NC-pin clearance errors as D13 PR-* per-instance justified, or
  require iter-9 to generate proper drc_exclusions via a KiCad GUI
  session (GUI-only path; needs user-side action).

user_pause_reasons_seen_this_run:
  - "iter-10 advance temporarily blocked by uncommitted pre-APPROVED WIP; user authorized merge; WIP stashed for CP5 evaluation per Q-CP5-2."
  - "iter-4 blocked: Freerouting v2.1.0 + v2.2.4 both hang on save-to-SES after autoroute completes; routing pipeline requires user direction. RESOLVED iter-5: user authorized 2-day solo session with all options available."
