# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: reviewer_turn
current_cp: cp4-display-placement
current_branch: hw/cp4-display-placement
active_packet: hardware/reviews/cp4_display_placement_review.md
iteration: 9

max_iterations_per_cp: 1000

# Reviewer Patch Authority (hardware/reviews/REVIEWER_PATCH_POLICY.md):
# git author names that count as the reviewer. A reviewer commit that
# changes code must carry a Reviewer-Patch trailer and satisfy the
# zero-delta invariant; enforced by tools/reviewer_patch_check.py.
reviewer_git_authors: [voxelisKW]
# Enforcement epoch: RPA binds commits from the policy's own commit
# forward. An immutable sha, never a mutable ref — a range like
# origin/main..HEAD makes the verdict depend on which clone runs it
# (CP3 F11). Pre-policy reviewer patches are legitimized retroactively
# by an RPA-ACCEPTED line, not by moving this.
rpa_policy_base: 3e4c097

last_updated_at: 2026-08-07T00:54:12Z
last_updated_by: claude-designer

note: >
  DESIGNER iteration 8: F11 and F12 closed. F11 - all three escapes were
  real. Both gates now read EMITTED board text, the round trip re-derives
  the anchor locally instead of calling the placer's own forward helper,
  and the body gate compares full TEXT BOXES against own and other
  same-side bodies. Poisoned all three with geometrically real cases; the
  strengthened gate then caught two LIVE defects the anchor-only version
  passed (J-USB and TVS2 designators on their own bodies) - fixed with
  explicit spots. Poisoning also found a FOURTH escape you did not list:
  the round trip silently skipped every ref when its selection dict was
  empty, so a run without the placer passed green - same class as the
  mounting holes that gated nothing. Now a hard finding.
  NEW INDEPENDENT ORACLE: pcbnew_crosscheck.py re-derives refdes
  positions, sides and pad->net bindings using KiCad's OWN engine at the
  write chokepoint, breaking the correlation that let F01/F09/F11 survive
  our own gates. Poisoned both directions; reports SKIPPED never PASS if
  KiCad's Python is absent. Display 39 refs/39 sides/97 pad-nets agree;
  it also independently validates the APPROVED battery board (123 refs,
  295 pad-nets, still byte-identical 448d59a276df).
  F12 - agreed: I imported the battery board's DR-31 CAN/JTAG rationale
  onto a board with NO CAN (verified zero TCAN/CAN_PWR refs) and counted
  J5, the RS-485 termination lift, as a debug header. PR-13 rewritten
  from this board's facts (J-USB native USB Serial/JTAG on dedicated
  pins; J3 ESP-Prog recovery); my iteration-6 response repeating the same
  claim is struck in place. Board 5b69b6c717a4.


user_pause_reasons_seen_this_run: []
