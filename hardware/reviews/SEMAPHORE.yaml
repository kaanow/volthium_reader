# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: reviewer_turn
current_cp: cp1-architecture
current_branch: hw/cp1-architecture
active_packet: hardware/reviews/cp1_design_baseline.md
iteration: 55

max_iterations_per_cp: 1000

last_updated_at: 2026-07-19T06:20:00Z
last_updated_by: claude-designer

note: >
  DESIGNER iter 54: all four iter-53 findings (F86-F89) ACCEPTED and FIXED,
  then self-reviewed to a clean pass before handing off.
  F86 (BLOCKER): §8.1 short-term-overload uses P70=0.75 W (verified p.8),
  so the guarantee is 4.69 W/5 s, not 9.4 W (1.5 W is only @105 C); the
  worst-case fault (29.2 V, R-1%, no fuse/SSR credit = 0.197 A) puts 5.74 W
  in a single 150 Ohm 1206-HP > 4.69 W. Fixed: R_inrush = 2x 75 Ohm 1206-HP
  in series (=150 Ohm), each 2.87 W < 4.69 W (guaranteed at P70); 2512-HP
  would also work but is not stocked at 150 Ohm. F87: F2 62->80 mA so the
  turn-on I2t is 19.1% of melt I2t (was 31.6%), under Littelfuse's
  <=20%/22%-at-100k limits; DR-28 now has a quantified <=100k-cycle /
  >=10s-cooling acceptance criterion. F88: R_inrush Mouser SKU ->
  71-CRCW120675R0FKEAH (resolve-exact; the bad 71-CRCW1206150RFKEAHP
  guarded in D35). F89: D35 F84 registry hint + passing fixture updated to
  the final design; added failing fixtures for the retired 220 Ohm /
  single-150 / bad-SKU; new F86-F89 registry row fires 11x on pre-fix HEAD,
  0x now. Fault clears F2 at 246% (214% cold) > 200%; inrush 0.66x SSR
  continuous. All numbers re-derived by the reviewer's worst-case method
  and propagated across cp1_bom, cp1_battery_side, decisions D19, docs BOM,
  block diagram, manifest (MPN rows), DR-28. D35 exit 0, fixtures pass.
  Seven self-review passes; the last two clean (pass 5 caught the 80 mA
  fuse Rcold 5.5->4.05 Ohm; pass 2 the 4->5-part/3x-1206 count). Packet 42.

user_pause_reasons_seen_this_run: []
