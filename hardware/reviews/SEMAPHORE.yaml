# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: claude_turn

current_cp: cp5-routing-drc
current_branch: hw/cp5-routing-drc
active_packet: hardware/reviews/cp5_routing_drc.md
iteration: 5

max_iterations_per_cp: 1000

last_updated_at: 2026-05-27T08:00:00Z
last_updated_by: claude

note: >
  Solo work session — user away ~2 days, codex offline. User authorized
  full autonomous CP5 progress: pick best routing approach, switch
  approaches if one isn't meeting expectations, download tools as
  needed, write pure-programmatic routing if needed. Goal: get as far
  as possible toward CP5 APPROVED in 2 days.

  Plan:
  1. Freerouting v2.x systematic flag exploration (one config wrote
     a .ses earlier — find what's different and replicate).
  2. Freerouting v1.9.0 fallback (different CLI architecture; user
     pre-authorized download this session).
  3. Pure-programmatic router via pcbnew.PCB_TRACK if Freerouting
     remains uncooperative.
  4. Whichever works → drive DRC toward zero, render, D11 inspect,
     hand back to codex on a stable iter-N checkpoint.

  Iter-2's net classes + B.Cu ground pour foundation is in place and
  was not re-blockered by codex iter-3, so it stands. Iter-4 user_turn
  resolution committed in this iter-5 update.

user_pause_reasons_seen_this_run:
  - "iter-10 advance temporarily blocked by uncommitted pre-APPROVED WIP; user authorized merge; WIP stashed for CP5 evaluation per Q-CP5-2."
  - "iter-4 blocked: Freerouting v2.1.0 + v2.2.4 both hang on save-to-SES after autoroute completes; routing pipeline requires user direction. RESOLVED iter-5: user authorized 2-day solo session with all options available (Freerouting variants, pure-programmatic, anything else needed)."
