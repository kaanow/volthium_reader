# Semaphore for the volthium_reader PCB design pass.
#
# Both agents (Claude = designer, Codex = reviewer) read this file FIRST
# on every invocation and only act if `state` matches their role.
# Protocols: REVIEWER.md (Codex) and DESIGNER.md (Claude).

schema_version: 1

state: codex_turn

current_cp: 2
current_branch: hw/cp2-schematic-capture
active_packet: hardware/reviews/cp2_schematic_capture.md
iteration: 19

max_iterations_per_cp: 30

last_updated_at: 2026-05-24T14:15:00Z
last_updated_by: claude

note: >
  Claude iter 18 on CP2 — MCU (MOD1 ESP32-S3-WROOM-1-N16R8) + ESP
  support. 41-pin module placed with all CP1 §6 connections labeled
  (11 connections per spec) and 28 unused GPIOs as NoConnect. Pin
  positions derived from the symbol library, no hardcoded coords.
  R7 (10k EN pull-up), C8 (1µF EN soft-start), C6 (10µF V3V3 bulk),
  C7 (100nF HF decoupling) added. Synthetic PWR_EN PWR_FLAG dropped
  (MOD1.IO4 is the real driver now); V3V3_SW PWR_FLAG added (MOD1.3V3
  is power_input, U1.SW is output not power_output). New ERC severity
  override: isolated_pin_label = "ignore" — many MOD1 pins are
  intentionally dangling this iter until RTC/U3/BTN/headers land in
  iter 20. CP2 close will re-enable and confirm. 24 components total,
  ERC 0/0. Next iter (20): RTC1, BAT1, U3, R10-13, TVS2, BTN1, J2,
  J3, J5 — completes battery-side.

user_pause_reasons_seen_this_run: []
