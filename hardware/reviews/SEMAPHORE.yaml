# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: claude_turn
current_cp: cp4-display-placement
current_branch: hw/cp4-display-placement
active_packet: hardware/reviews/cp4_display_placement_review.md
iteration: 8

max_iterations_per_cp: 1000

# Reviewer Patch Authority (hardware/reviews/REVIEWER_PATCH_POLICY.md):
# git author names that count as the reviewer. A reviewer commit that
# changes code must carry a Reviewer-Patch trailer and satisfy the
# zero-delta invariant; enforced by tools/reviewer_patch_check.py.
reviewer_git_authors: [voxelisKW]
# Enforcement epoch: RPA binds commits from the policy's own commit
# forward. An immutable sha, never a mutable ref — a range like
# origin/main..HEAD makes the verdict depend on which clone runs it
# (CP3 F11). Pre-policy reviewer patches are legitimized retroactively
# by an RPA-ACCEPTED line, not by moving this.
rpa_policy_base: 3e4c097

last_updated_at: 2026-08-06T21:13:45Z
last_updated_by: agent-reviewer

note: >
  REVIEWER iteration 7: NEEDS CHANGES - 0 blockers, 2 important. The actual
  F09 J1 repair passes fresh rebuild, serialized-board probe, independent
  AABB audit, and crop-zoom inspection. F11: the new round-trip consumes
  in-memory overrides plus its coupled forward helper rather than emitted
  board text, while the body gate checks only anchors and skips own bodies;
  all three independent poisons escape. Parse emitted reference anchors and
  full text boxes, then gate both own and other same-side bodies. F12: PR-13
  imports the battery board's DR-31 CAN/JTAG rationale into the display board;
  display J-USB is native USB-JTAG, J3 is ESP-Prog recovery, and J5 is only
  the RS-485 termination jumper. Full handoff CLEAN, direct DRC accounted,
  four source PDFs checked. CP5 not started. Evidence under
  visual_inspections/cp4-display-placement/iter7/reviewer/.


user_pause_reasons_seen_this_run: []
