# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: reviewer_turn

current_cp: cp1-architecture
current_branch: hw/cp1-architecture
active_packet: hardware/reviews/cp1_design_baseline.md
iteration: 31

max_iterations_per_cp: 1000

last_updated_at: 2026-07-16T15:30:00Z
last_updated_by: claude

note: >
  CLAUDE iter 30: all 7 iter-29 findings addressed — responses §28.
  F28: ADM2587E ICC = 90 mA @ 3.3 V/100 Ω (was mis-cited 72 mA = 5 V row);
  re-derived gated avg ~1.5 mW; fixed every live site + registry.
  F29: SM712 on-file PDF is SMC but the ordered 947-SM712.TCT is Semtech
  → D32 kept OPEN, DNP-by-default, Semtech datasheet is a user-provide
  (Salesforce blocks the proxy). F30: shared-UART off-state redesigned —
  dedicated per-channel DI/DE/RO, matrix-muxed UART2, inactive pins
  high-Z (§2a); no reliance on unspecified VCC=0 behavior; 5→8 GPIOs.
  F31: worst-case DC common-mode analysis added (offset absorbed across
  the 2500 V barrier); A/B-vs-TTL promoted to a hard pre-freeze bench
  gate. F32: full isoPower support network enumerated (p.8/p.17) + CP2/
  CP3 isolation-keepout contract. F33: PicoBlade datasheets on file; DK
  SKU corrected WM7626(r/a)→WM7612CT-ND(vert). F34: expansion rail now
  load-switched, default-OFF + force-OFF in State 4 (Q_exp). D35 gate
  exit 0 (addendum now in scope). CP2 stays gated on the user's go.

user_pause_reasons_seen_this_run: []
