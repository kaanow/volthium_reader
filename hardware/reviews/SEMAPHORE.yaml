# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: reviewer_turn

current_cp: cp1-architecture
current_branch: hw/cp1-architecture
active_packet: hardware/reviews/cp1_design_baseline.md
iteration: 39

max_iterations_per_cp: 1000

last_updated_at: 2026-07-17T21:30:00Z
last_updated_by: claude

note: >
  CLAUDE iter 38: all 4 iter-37 findings addressed — responses §33.
  F56/F57: ZXMP6A13F (NRND, no RDS(on) guarantee below -4.5 V) replaced
  everywhere — Q1 = Vishay Si2309CDS (-60 V, RDS 0.345R max @ -10 V
  clamped drive, VGS +/-20 V, IDSS specced at 55 C; DK 120k Active);
  3V3 switches x3 = Diodes DMG3415U (RDS guaranteed 53 mR max @ -2.5 V
  -> the 3.3 V direct drive is a QUALIFIED operating point; DK 136k
  Active). Both datasheets proxy-fetched, read, manifested; ZXMP PDF
  retired. Note of record: the -3.3 V operating point predates iter-36
  (the 2N7002 shifter also only delivered -3.3 V) — F51 exposed it, not
  created it. F58: all 3 live bleed forms rewritten; 2-wire property now
  CONDITIONAL on the §7 matrix; REF_pad row added (direct 0R link — the
  unbounded "through ~100k" phrase withdrawn); regex widened to 4
  shorthand families. F59: full molex URL in manifest. Delta ~$57.4 /
  total ~$249.4. 5 new registry rows verified FIRING (44 pre-fix hits);
  gate caught 3 stragglers itself incl. REVIEWER.md's part list; the
  AO3401A row's current token chain-updated (noted in-code). D35 exit 0.
  On-site two-domain matrix (~2 wks) still gates D36/DR-26.
  ADDENDUM (same handoff, pre-review): owner bench-characterized the
  purchased vendor cables — connector = 4-pos M12-pattern (~12.3 mm
  thread, insert numbered 1-4 CCW; 2 photos COMMITTED docs/vendor/
  images/); full conductor map measured: battery 1/2=CAN-H/L -> RJ45
  4/6 (label's 4/5 is a different product — F45 vindicated), battery
  3/4 = A/B -> RJ45 7/8 (third confirmation); cable ends RJ45-MALE ->
  direct mate, no patch cable. Docs folded (packet §33.1); 4 more
  registry rows, all verified firing. On-site list now: matrix +
  which-socket only. D35 exit 0.

user_pause_reasons_seen_this_run: []
