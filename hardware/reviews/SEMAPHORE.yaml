# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: codex_turn

current_cp: cp4-display-placement
current_branch: hw/cp4-display-placement
active_packet: hardware/reviews/cp4_display_placement.md
iteration: 5

max_iterations_per_cp: 1000

last_updated_at: 2026-05-26T22:20:00Z
last_updated_by: claude

note: >
  iter-4 work complete. Two iter-3 findings addressed.
  (04 blocker — DISAGREE) D11 iter-2 evidence IS committed on
  origin/hw/cp4-display-placement at the paths the packet
  references; verified via `git ls-tree -r origin/...` showing
  all 10 PNGs from commit 8f55759. Counter-evidence pasted in §9.4
  RESOLVED entry. Counter-proposal: please re-verify against the
  branch tip, not main. iter-4 also adds MANIFEST.sha256 under
  iter4/ for deterministic reviewer verification going forward.
  (05 important — AGREE + extra). Two root causes addressed:
  (a) net-correctness audit revealed iter-2 misidentified
  C5/C8/C9/C10 as decoupling caps when the netlist shows they
  are button-debounce and ESP_EN debounce caps. Caps moved to
  their correct paired locations (R+C per button, R1+C5 at MOD1
  EN). (b) R3/R4 moved 6mm right of U2 anchor (was 4mm) to clear
  U2 pads 6/7 solder-mask web. Result: 0 solder_mask_bridge
  (was 5), 0 hole_clearance errors (was 1), 93 total violations
  (was 112). See cp4_display_placement.md §9.4 + the new D11 iter4
  inspection section.

user_pause_reasons_seen_this_run:
  []
