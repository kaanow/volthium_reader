# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: reviewer_turn
current_cp: cp3-placement
current_branch: hw/cp3-placement
active_packet: hardware/reviews/cp3_placement_review.md
iteration: 15

max_iterations_per_cp: 1000

# Reviewer Patch Authority (hardware/reviews/REVIEWER_PATCH_POLICY.md):
# git author names that count as the reviewer. A reviewer commit that
# changes code must carry a Reviewer-Patch trailer and satisfy the
# zero-delta invariant; enforced by tools/reviewer_patch_check.py.
reviewer_git_authors: [voxelisKW]
# Enforcement epoch: RPA binds commits from the policy's own commit
# forward. An immutable sha, never a mutable ref — a range like
# origin/main..HEAD makes the verdict depend on which clone runs it
# (CP3 F11). Pre-policy reviewer patches are legitimized retroactively
# by an RPA-ACCEPTED line, not by moving this.
rpa_policy_base: 3e4c097

last_updated_at: 2026-08-05T20:59:15Z
last_updated_by: claude-designer

note: >
  DESIGNER iteration 14: F15 fact AGREED and independently verified
  (author names are free-form; project has no signing, all commits
  %G?=N); docstrings corrected where they overstated authorship as
  authentication. SEVERITY DISPUTED — not resolved, deferred to the
  user as DR-34. The artifact this thread was missing is a THREAT
  MODEL, now in REVIEWER_PATCH_POLICY.md: one principal, both agents
  with full repo write access; RPA prevents ACCIDENT, and cannot
  defend against a subverter who has strictly easier attacks (rewrite
  the gate, commit design data, push to main). Termination rule:
  defeatable-by-easier-means = note; fails-open-by-accident = blocker.
  F11-F14 were the latter and are fixed at root; F15 is the former.
  Signing needs a key only the user can provision, and its premise (a
  credential the reviewer cannot reach) is a fact about their machines
  I cannot verify. PROPOSAL in 9.8: judge CP3 on the placement — board
  blob 448d59a276df is byte-identical across iterations 4-13 and every
  design gate is green — with RPA hardening continuing non-blocking
  under DR-34. If you disagree, say why and I take it to the user as
  a consensus failure (DESIGNER.md 8b).


user_pause_reasons_seen_this_run: []
