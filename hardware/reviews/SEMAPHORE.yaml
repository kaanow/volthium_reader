# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: reviewer_turn

current_cp: cp1-architecture
current_branch: hw/cp1-architecture
active_packet: hardware/reviews/cp1_design_baseline.md
iteration: 1

max_iterations_per_cp: 1000

last_updated_at: 2026-06-19T00:00:00Z
last_updated_by: claude

note: >
  CP1 ARCHITECTURE — handoff to the reviewer (iteration 1). THIS ROUND IS A
  SINGLE THOROUGH PASS, not an automated loop: do one deep review, write
  findings into a new §8 of cp1_design_baseline.md, then hand back to the
  USER (state: user_turn) — do NOT flip to claude_turn. A human reads your
  pass before the designer responds.

  Read cp1_design_baseline.md §5 first — it is the designer's prioritized
  request for this review (independent re-derivation of the D19 power-tree
  protection coordination; part reality/variant + LM5166 fixed-vs-adjustable;
  the domain the designer may have skipped, esp. regulator thermals and the
  display depth stack; the DR-6 sense-divider/ADC source-impedance subtlety;
  D18–D27 / DR-1..DR-11 internal consistency). Re-derive; don't trust the
  RESOLVED tags. Skip readability/D11/D13 (no schematic yet — CP2) and the
  staleness re-audit (a clean mechanical sweep was just run).

  Context: the full clean-sheet engineering review (ENGINEERING_REVIEW.md /
  D17) was run across all domains on both boards; findings landed as D18–D27
  and DR-1..DR-11 (all RESOLVED). Board artifacts (CP3–CP6) remain superseded
  per D18; the generators (build_*.py) are intentionally still pre-D19 — CP2
  work, not a CP1 finding.

  Authoritative entry points: hardware/layout/decisions.md,
  hardware/layout/cp1_{battery,display}_side.md, hardware/layout/cp1_bom.md,
  hardware/reviews/DESIGN_REVIEW_ITEMS.md. Reviewer instructions: REVIEWER.md.
