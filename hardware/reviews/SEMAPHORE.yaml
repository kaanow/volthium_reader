# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: reviewer_turn
current_cp: cp3-placement
current_branch: hw/cp3-placement
active_packet: hardware/reviews/cp3_placement_review.md
iteration: 9

max_iterations_per_cp: 1000

# Reviewer Patch Authority (hardware/reviews/REVIEWER_PATCH_POLICY.md):
# git author names that count as the reviewer. A reviewer commit that
# changes code must carry a Reviewer-Patch trailer and satisfy the
# zero-delta invariant; enforced by tools/reviewer_patch_check.py.
reviewer_git_authors: [voxelisKW]

last_updated_at: 2026-07-31T00:18:03Z
last_updated_by: claude-designer

note: >
  DESIGNER iteration 8 (amended): F10 fix shipped (hemmed-in Windows
  transient-EINVAL retry + transactional -o unlink + kcli routing for
  drc/render/svg; 7-case poison suite in iter8/einval_poison.txt;
  artifacts byte-identical 448d59a276df / eda5076694c0).
  PROCESS CHANGE (user-directed): Reviewer Patch Authority is now
  standing policy — REVIEWER_PATCH_POLICY.md + tools/
  reviewer_patch_check.py, wired into handoff_check. You MAY now fix
  host-limited defects directly: lone commit, Reviewer-Patch +
  Patch-Reason trailers, host evidence, and ZERO design-artifact
  delta; designer re-reviews and signs off by sha. F10 carries an
  explicit HOST-LIMITED invitation (packet 9.4) — patch it on Windows
  rather than filing another round-trip if my retry doesn't hold.
  Drill transcript: iter8/rpa_drill.txt. Skills: pcb-design-review
  v1.5.0, pcb-design v0.12.0, kicad v0.3.5.


user_pause_reasons_seen_this_run: []
