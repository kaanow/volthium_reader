# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: reviewer_turn
current_cp: cp4-display-placement
current_branch: hw/cp4-display-placement
active_packet: hardware/reviews/cp4_display_placement_review.md
iteration: 7

max_iterations_per_cp: 1000

# Reviewer Patch Authority (hardware/reviews/REVIEWER_PATCH_POLICY.md):
# git author names that count as the reviewer. A reviewer commit that
# changes code must carry a Reviewer-Patch trailer and satisfy the
# zero-delta invariant; enforced by tools/reviewer_patch_check.py.
reviewer_git_authors: [voxelisKW]
# Enforcement epoch: RPA binds commits from the policy's own commit
# forward. An immutable sha, never a mutable ref — a range like
# origin/main..HEAD makes the verdict depend on which clone runs it
# (CP3 F11). Pre-policy reviewer patches are legitimized retroactively
# by an RPA-ACCEPTED line, not by moving this.
rpa_policy_base: 3e4c097

last_updated_at: 2026-08-06T20:51:47Z
last_updated_by: claude-designer

note: >
  DESIGNER iteration 6: F09 and F10 closed. F09 - the instruction to
  round-trip rather than guess an axis is what found the real defect. The
  round trip showed my board-space computation put J1 at 29.44,47.32 vs
  your measured 11.000,47.325; of three candidate transforms only NO
  MIRROR reproduces yours. Structural reason: _flip_to_back already
  mirrors pads/graphics INTO the file and the Reference property is
  substituted afterwards, so it is applied with no further mirror - two
  frames, one rule wrongly applied to both. The round trip then exposed
  that auto_refdes carried TWO hand-rolled inverses and my earlier fix
  corrected only one; J1 goes through the other. Same duplication class
  as F08, inside one function. Now one owner, refdes_board_to_local,
  with a forward partner. Two gates added at the write chokepoint:
  assert_refdes_roundtrip (emitted anchor == chosen board point) and
  refdes_over_body_findings (reference text vs other bodies, same side).
  Poisoned on your exact case - reinstating the mirror reproduces
  11.00,47.32 inside U1 - control clean; re-rendered, J1 reads clear.
  PR-1/PR-2 corrected FROM EVIDENCE rather than re-asserted. F10: PR-13
  added with evidence, including explicitly recording that there is no
  SWD/JTAG because those pins were forfeited to the CAN gate (DR-31).
  Board dc28b2fe36e6; battery byte-identical 448d59a276df.


user_pause_reasons_seen_this_run: []
