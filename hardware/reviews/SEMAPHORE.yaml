# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: reviewer_turn

current_cp: cp1-architecture
current_branch: hw/cp1-architecture
active_packet: hardware/reviews/cp1_design_baseline.md
iteration: 29

max_iterations_per_cp: 1000

last_updated_at: 2026-07-16T00:30:00Z
last_updated_by: claude

note: >
  CP1-DELTA REVIEW (iter 29) — CP1 was APPROVED + audit-clean at iter-27.
  Since then: (a) branch rebased onto main (40 upstream commits; one
  combined conflict in production_design.md, resolved); (b) two
  user-directed architecture additions to review before CP2. Packet §27
  bundles them. D36/DR-26: RS-485 wired battery read as a **co-equal
  alternative transport** (may become primary — not pre-judged); series
  stack + polled protocol → two per-pack galvanically-isolated channels
  (ADM2587E), shared UART2, power-gated select, symmetric/interchangeable,
  self-referenced (no battery-negative wire); hard-cut ~1.08 mW unchanged.
  D37/DR-27: battery-side PicoBlade expansion header (dedicated I2C1 + 2×
  ADC1/RTC-wake AIO + DIO + power). Review asks in §27.2 + DR-26/DR-27.
  **D32 CLOSED 2026-07-16:** ADM2587E + SM712 datasheets on file
  (user-provided), read/verified — ADM2587E ICC 72 mA @ 3.3 V drove a
  power correction (§6 ~1–1.5 mW gated avg; hard-cut unchanged), SM712
  now DNP-by-default (on-chip ±15 kV ESD). D35 gate exit 0. CP2 stays
  gated on the user's go regardless of outcome.

user_pause_reasons_seen_this_run: []
