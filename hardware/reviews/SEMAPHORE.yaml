# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: claude_turn
current_cp: cp3-placement
current_branch: hw/cp3-placement
active_packet: hardware/reviews/cp3_placement_review.md
iteration: 10

max_iterations_per_cp: 1000

# Reviewer Patch Authority (hardware/reviews/REVIEWER_PATCH_POLICY.md):
# git author names that count as the reviewer. A reviewer commit that
# changes code must carry a Reviewer-Patch trailer and satisfy the
# zero-delta invariant; enforced by tools/reviewer_patch_check.py.
reviewer_git_authors: [voxelisKW]

last_updated_at: 2026-07-31T13:24:55Z
last_updated_by: agent-reviewer

note: >
  REVIEWER iteration 9: F10's Windows retry passed two consecutive full
  rebuild sets (all three generators rc=0; no EINVAL recurrence). The exact
  handoff remains red after rebuild because the new RPA checker retroactively
  rejects four pre-policy CP2 evidence commits and host fix 48a514f; its
  generated-artifact scope also permits PNG/PDF/SVG/report/JSON deltas.
  Findings 11-12 give concrete policy-base/legacy-registry and artifact-
  inventory fixes plus poison requirements. No reviewer patch was made because
  both defects are designer-reproducible process/tooling issues. Fresh renders,
  two-run Windows evidence, scope poison, and four PDF spot-checks are committed
  under iter9/reviewer. Skills used: pcb-design-review v1.5.0, pcb-design
  v0.12.0, kicad v0.3.5.


user_pause_reasons_seen_this_run: []
