# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: claude_turn
current_cp: cp4-display-placement
current_branch: hw/cp4-display-placement
active_packet: hardware/reviews/cp4_display_placement_review.md
iteration: 10

max_iterations_per_cp: 1000

# Reviewer Patch Authority (hardware/reviews/REVIEWER_PATCH_POLICY.md):
# git author names that count as the reviewer. A reviewer commit that
# changes code must carry a Reviewer-Patch trailer and satisfy the
# zero-delta invariant; enforced by tools/reviewer_patch_check.py.
reviewer_git_authors: [voxelisKW]
# Enforcement epoch: RPA binds commits from the policy's own commit
# forward. An immutable sha, never a mutable ref — a range like
# origin/main..HEAD makes the verdict depend on which clone runs it
# (CP3 F11). Pre-policy reviewer patches are legitimized retroactively
# by an RPA-ACCEPTED line, not by moving this.
rpa_policy_base: 3e4c097

last_updated_at: 2026-08-07T03:05:07Z
last_updated_by: agent-reviewer

note: >
  REVIEWER iteration 9: NEEDS CHANGES - 0 blockers, 2 important. F11's
  emitted-text round-trip/body gates now pass independent clean control,
  catch all four poisons, and parse MOD1 at 18.00x25.50 mm; F12 PR-13 is
  corrected to this board's J3 ESP-Prog and J-USB native USB-JTAG paths.
  F13: the new pcbnew oracle samples only the first four pins per component
  (97 of 191 net-bound pads), and a required missing pad passes while being
  counted as agreement. Check every expected pad and fail if none exists.
  F14: iter8/refdes_gate_poison_v2.txt still calls J-USB/TVS2 real defects
  fixed by MANUAL_REFDES, contradicting the packet retraction and current
  empty override map; supersede/regenerate it and gate the stale phrase.
  Bare full handoff CLEAN, direct DRC accounted, four source PDFs checked.
  CP5 not started. Evidence under visual_inspections/cp4-display-placement/
  iter9/reviewer/.


user_pause_reasons_seen_this_run: []
