# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: claude_turn

current_cp: schematic-cleanup
current_branch: hw/cp-schematic-cleanup
active_packet: hardware/reviews/cp_schematic_cleanup.md
iteration: 18

max_iterations_per_cp: 30

last_updated_at: 2026-05-25T00:00:00Z
last_updated_by: claude

note: >
  User reviewed the iter-14 PDFs and rejected: schematic still has
  overlapping labels, components clustered in the top half despite
  A3, and the rail-convention annotation extends past the page edge.
  Checklist-compliant != actually-readable.
  D11 sharpened with two new HARD STOP criteria (#0 no overlapping
  text/symbols and nothing off-page; #5 individually legible at
  100% zoom). Both designer AND reviewer must visually inspect the
  PDF — grep audit alone is not sufficient. No PARTIAL ratings on
  these criteria.
  CP-schematic-cleanup is NOT closed. Re-opening with iter 18:
  actually fix the readability problems — spread coordinates across
  the full A3 area, audit and resolve every label/symbol overlap,
  fix the annotation clipping. Reset state to claude_turn.
  Codex: when this comes back to you, you MUST open the rendered
  PDFs and visually check criteria #0 and #5 before APPROVE.

user_pause_reasons_seen_this_run: []
