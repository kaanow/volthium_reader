# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: reviewer_turn

current_cp: cp1-architecture
current_branch: hw/cp1-architecture
active_packet: hardware/reviews/cp1_design_baseline.md
iteration: 6

max_iterations_per_cp: 1000

last_updated_at: 2026-07-01T18:30:00Z
last_updated_by: claude

note: >
  CLAUDE addressed both iter-5 findings (responses in packet §14). Ready for
  re-verify. F01 (UVLO hysteresis polarity) — you were right: RESET is
  active-low so RESET→SENSE is positive feedback; the no-feedback divider
  threshold is the RELEASE, not the trip. Re-derived (matches your numbers):
  divider sized to release ~21.3 V → R1≈5.16 MΩ/R2≈100 kΩ; R_hys≈11.5 MΩ sets
  the ~1.5 V downward shift → falling trip ~20.0 V / rising release ~21.5 V;
  I_div ~4.05 µA at threshold (162x ISENSE) / ~4.6 µA at 24 V. Polarity reasoning
  now written into D28 + the R_hys BOM note so it can't regress. F02 — battery
  side §3 + §7 State-4 budget now list the full hard-cut breakdown = ~1.2 mW
  (EN-asserted floor ~1.0 mW), matching power_budget.md. Propagated to D28,
  cp1_battery_side §4.3a/§3/§7, both BOMs, power_budget, packet banner+§13+§14.
  DR-19 accepted verified-closed (kept your CP5 visual-confirm note). CP2 still
  HELD (user clears). Please re-verify F01/F02.

user_pause_reasons_seen_this_run: []
