# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: reviewer_turn

current_cp: cp1-architecture
current_branch: hw/cp1-architecture
active_packet: hardware/reviews/cp1_design_baseline.md
iteration: 2

max_iterations_per_cp: 1000

last_updated_at: 2026-06-22T12:00:00Z
last_updated_by: claude

note: >
  REVIEWER — comprehensive single verification pass (likely final for CP1).
  Everything outstanding since your iter-1 §8 is now on the table. Read, in
  order: packet §9 (Claude's responses to your iter-1 findings 01–08),
  §10 (iter-3 brief: verify DR-12…DR-16 + a board-wide abs-max-vs-worst-case
  sweep on BOTH boards + ESP strap check + datasheet-support check), and
  §11 (iter-4 brief: verify DR-17…DR-23). Re-derive independently per
  ENGINEERING_REVIEW.md / D17 — do NOT trust the RESOLVED tags.

  Write all findings into a NEW §8 subsection ("iteration 2"), end with a
  sign-off line, then hand back to **user_turn** (NOT claude_turn) — this is
  a single pass; do not loop. Tag each finding blocker / should-fix / nit
  with concrete numbers.

  Headline things to re-derive (highest value first): the D19 power-tree
  clamp coordination (53.3 V vs the 60 V floor parts); the D28 hardware UVLO
  (U4 TPS3890 EN-assert → MCU reset + display auto-shed; brownout vs UVLO
  ordering; EN-gating preserves the floor regardless of supply); LM5166
  **Y**DRCR = 3.3 V (X = 5 V); DR-18 VBUS-off-V3V3 on both boards.

  USER decisions already made — VERIFY the engineering, do NOT reopen:
  DR-16 accepted (add UVLO), DR-21 accepted (UVLO fail-to-baseline, no
  self-test), DR-22 accepted (e-paper 0 °C floor, no heater), DR-18 default
  (no USB-power OR). Skip readability/D11/D13 (no schematic — that's CP2) and
  the staleness re-audit (clean sweep already run). CP2 remains HELD pending
  the user; board generators (build_*.py) are intentionally still pre-D19.

user_pause_reasons_seen_this_run: []
