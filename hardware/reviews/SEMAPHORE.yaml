# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: user_turn

current_cp: cp1-architecture
current_branch: hw/cp1-architecture
active_packet: hardware/reviews/cp1_design_baseline.md
iteration: 48

max_iterations_per_cp: 1000

last_updated_at: 2026-07-18T14:00:00Z
last_updated_by: claude

note: >
  ESCALATION TO USER (Claude iter 48, packet §38). The Q1 gate-driver
  OFF-guarantee has been re-opened 5 iterations running (F60→F64→F68→
  F72→F76). F76/F77 are valid: no single discrete transistor in the
  mainstream catalog meets "datasheet-guaranteed OFF at 100 °C in the
  grounded config" — 30 V logic-level FETs have the hot-IDSS spec but
  can't take the 29 V node; 40–60 V FETs aren't logic-level and lack a
  hot-IDSS row; the BJT has no guaranteed grounded-emitter cutoff hot.
  This is a genuine cost/complexity/risk tradeoff (design intent), not a
  fixable slip. User to choose: (A) accept an engineering-reasoned OFF
  bound + declared ≤60 °C enclosure range (consistent with the project's
  own 'max-where-spec'd + engineering-margin-where-not' convention; $0);
  (B) logic-level-FET + HV cascode (guaranteed; +1 FET + bias); or
  (C) opto-MOSFET / photoMOS relay replacing Q1 + the whole gate network
  (guaranteed OFF-leakage + ON; ~$1–2 + ~5 mA active LED drive). F78
  propagation cleanup deferred until the design is chosen. Reviewer:
  please hold; do not re-review until state returns to reviewer_turn.

user_pause_reasons_seen_this_run: ["gate-driver OFF-guarantee: discrete parts cannot meet the datasheet-guaranteed-hot bar; user decision on approach A/B/C"]
