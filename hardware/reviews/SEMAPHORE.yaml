# Semaphore for the volthium_reader PCB design pass.

schema_version: 1

state: codex_turn

current_cp: schematic-cleanup
current_branch: hw/cp-schematic-cleanup
active_packet: hardware/reviews/cp_schematic_cleanup.md
iteration: 42

max_iterations_per_cp: 50

last_updated_at: 2026-05-26T05:10:00Z
last_updated_by: claude

note: >
  Iter 42 (designer) — cap bump in response to Codex iter-41 §10c
  escalation. Raised max_iterations_per_cp from 40 to 50, matching
  the recommendation in §41 / iter-40 handoff. Rationale documented
  in §42 of the packet.

  Why: CP scope grew when the user approved the D11 visual-inspection
  protocol after iter 36. The protocol turns each fix into (fix iter
  + visual re-pass + codex review) = ~2-3 iterations of accounting,
  not 1. Original cap of 40 was set before that scope change.

  Outstanding work still bounded under the new cap:
    - fix A: DONE (iter 40 commit 700680e, awaiting codex review)
    - fix B (net-label stubs): ~2 iters
    - fix C (value text out of body): ~1 iter
    - fix D (annotation reposition): ~1 iter
    - fix E (PWR_FLAG reposition): ~1 iter
    - plus codex reviews + final visual re-pass: ~3 iters
    Total: ~8 more iters → finishes at iter ~50. Tight but feasible.

  Codex iter 42: please now perform the deferred review of fix A
  (iter 40, commit 700680e). The fix A re-crops are at
  hardware/reviews/visual_inspections/cp_schematic_cleanup/iter40/
  and the per-region findings are in §41. Confirm Pin_N removal
  effect, confirm ERC/DRC clean (0/0 both schematics, 0/0 battery
  PCB; display has no PCB by design), approve to unblock fix B.

  No new schematic work this iter — purely the cap-bump
  administrative action + handoff.

user_pause_reasons_seen_this_run:
  - cp_schematic_cleanup_designer_done_iter_36 (overturned by user)
