  [advisory] same-net labels 'GND' only 2.5 mm apart ((230,44)/(230,42)) — consider wiring instead of two flags
  [advisory] same-net labels 'GND' only 2.5 mm apart ((230,47)/(230,44)) — consider wiring instead of two flags
  [advisory] same-net labels 'V12_CAT5E' only 2.5 mm apart ((230,57)/(230,55)) — consider wiring instead of two flags
  [advisory] same-net labels 'V12_CAT5E' only 2.5 mm apart ((230,60)/(230,57)) — consider wiring instead of two flags
  [advisory] same-net labels 'GND' only 5.1 mm apart ((230,47)/(230,42)) — consider wiring instead of two flags
  [advisory] same-net labels 'V12_CAT5E' only 5.1 mm apart ((230,60)/(230,55)) — consider wiring instead of two flags
  [advisory] same-net labels 'EXP_3V3' only 10.2 mm apart ((150,124)/(150,114)) — consider wiring instead of two flags
  [advisory] same-net labels 'GND' only 13.0 mm apart ((110,127)/(98,130)) — consider wiring instead of two flags
  [advisory] same-net labels 'EXP_3V3' only 14.8 mm apart ((168,136)/(180,144)) — consider wiring instead of two flags
  [advisory] same-net labels 'GND' only 14.8 mm apart ((168,149)/(180,141)) — consider wiring instead of two flags
  [advisory] same-net labels 'GND' only 16.3 mm apart ((168,149)/(180,159)) — consider wiring instead of two flags
  [advisory] same-net labels 'GND' only 17.8 mm apart ((180,141)/(180,159)) — consider wiring instead of two flags
  [advisory] same-net labels 'GND' only 18.5 mm apart ((47,150)/(42,132)) — consider wiring instead of two flags
=== sheet_conn.kicad_sch: 0 geometry findings ===
  [advisory] same-net labels 'V12_CAT5E' only 2.5 mm apart ((225,65)/(225,62)) — consider wiring instead of two flags
  [advisory] same-net labels 'GND' only 2.5 mm apart ((225,52)/(225,50)) — consider wiring instead of two flags
  [advisory] same-net labels 'GND' only 2.5 mm apart ((225,55)/(225,52)) — consider wiring instead of two flags
  [advisory] same-net labels 'V12_CAT5E' only 2.5 mm apart ((225,67)/(225,65)) — consider wiring instead of two flags
  [advisory] same-net labels 'GND' only 5.1 mm apart ((225,55)/(225,50)) — consider wiring instead of two flags
  [advisory] same-net labels 'V12_CAT5E' only 5.1 mm apart ((225,67)/(225,62)) — consider wiring instead of two flags
  [advisory] same-net labels 'GND' only 13.0 mm apart ((110,127)/(98,130)) — consider wiring instead of two flags
  [advisory] same-net labels 'GND' only 18.5 mm apart ((47,150)/(42,132)) — consider wiring instead of two flags
=== sheet_d_conn.kicad_sch: 0 geometry findings ===
