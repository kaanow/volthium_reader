fastapi>=0.115
uvicorn[standard]>=0.30
asyncpg>=0.30
pydantic>=2.7
python-dotenv>=1.0
# For the staleness monitor's webhook POST. NOT optional — cloud/server/staleness.py
# imports it at module load, so a Railway image without httpx fails to start
# and the healthcheck loops on 502 (regression 2026-07-01).
httpx>=0.27
